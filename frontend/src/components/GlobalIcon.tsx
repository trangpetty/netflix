import { FontAwesomeIcon, FontAwesomeIconProps } from '@fortawesome/react-fontawesome';

const GlobalIcon = (props: FontAwesomeIconProps) => {
    return <FontAwesomeIcon {...props} />;
};

export default GlobalIcon;
